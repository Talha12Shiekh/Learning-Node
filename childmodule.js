const cp = require("child_process");

// cp.execSync("start chrome talhawebsite.netlify.app")
// cp.execSync("calc")

// console.log(cp.execSync("dir"))

// cp.execFileSync("./main.js")
// cp.spawnSync("calc")
// cp.execFile('node', ['--version'], (error, stdout, stderr) => {
//     if (error) {
//       throw error;
//     }
//     console.log(stdout);
//   }); 